# Go URL Shortener

A lightweight URL Shortener built in Go that converts long URLs into short, unique links. It provides RESTful endpoints for URL shortening and redirection while leveraging Go's standard `net/http` library. This project showcases fundamental backend concepts such as HTTP routing, JSON handling, and REST API development.